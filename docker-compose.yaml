version: "3.8"

services:
  prefect-server:
    image: prefecthq/prefect:3-latest
    container_name: prefect-server
    restart: always
    command: prefect server start --host 0.0.0.0 --port 4200
    ports:
      - "4200:4200"
    volumes:
      - ./volumes/prefect:/root/.prefect
    
  prefect-worker:
    image: prefecthq/prefect:3-python3.11
    container_name: prefect-worker
    restart: always
    volumes:
      - ./requirements.txt:/requirements.txt
      - ./filesharing:/var/lib/filesharing
      - ./.env:/.env
    command: >
      bash -c "pip install --no-cache-dir -r /requirements.txt && 
      prefect worker start --pool 'default-agent-pool' --type process"
    environment:
      - PREFECT_API_URL=http://prefect-server:4200/api
      - PYTHONPATH=/opt/prefect/deployment_prefect
    depends_on:
      - prefect-server

  mysql:
    image: mysql:8.0
    container_name: mysql_db
    restart: unless-stopped
    command: --default-authentication-plugin=mysql_native_password --skip-ssl --secure-file-priv=/var/lib/filesharing
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - ./volumes/mysql/init:/docker-entrypoint-initdb.d
      - ./filesharing:/var/lib/filesharing